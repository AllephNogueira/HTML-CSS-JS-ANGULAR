const numero = 7;
const multiplicadorMaximo = 10

for (let index = 1; index <= multiplicadorMaximo; index++) {
    const calculo = numero * index;
    console.log(`${numero} * ${index} = ${calculo}`)
    
}